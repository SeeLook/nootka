#! /bin/bash

CALL=$(echo `dirname $0`)

if [ -f /usr/lib/libQtMultimediaKit.so ]; then
  $CALL/bin/nootka-kit
else
  if [ -f /usr/lib/libQtMultimedia.so ]; then
	$CALL/bin/nootka-nokit
  else
	echo "Can not find QtMultimedia libraary"
  fi
fi

